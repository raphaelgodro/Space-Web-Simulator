goog.provide('entropy.interaction.ArrowInteraction');



/**
 * @enum {string}
 * @api stable
 */
/*entropy.media.ImageWidthType = {
  FULL: 'full',
  PX: 'pixel'
};*/

/**
 * Menu that can set properties of a corp dynamcally
 * (Object that has the movement physically calculed.)
 * @classdesc
 * @constructor
 */
entropy.interaction.ArrowInteraction =
    function(camera) {
  console.log(camera);
}


/**
 * Get the element of the media Image
 * @return {element}
 */
entropy.interaction.ArrowInteraction = function() {
  return this.el_;
}